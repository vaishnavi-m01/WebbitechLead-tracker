const fs = require("fs"); const q = String.fromCharCode(96); const content = `/**\n * exportUtils.ts - Reusable export helpers\n * - PDF color: #7C3AED (purple)\n * - Download notifications for PDF, Excel, CSV\n * - Bug fix: share from cache (not media store URI) to prevent Android getScheme() crash\n */\n\nimport { Platform, PermissionsAndroid, ToastAndroid, Clipboard } from "react-native";\nimport RNFS from "react-native-fs";\nimport Share from "react-native-share";\nimport RNBlobUtil from "react-native-blob-util";\nimport { generatePDF } from "react-native-html-to-pdf";\nimport * as XLSX from "xlsx";\n\nexport const PDF_ACCENT = "#7C3AED";\n\nexport const requestStoragePermission = async (): Promise<boolean> => {\n  if (Platform.OS !== "android" || Platform.Version >= 29) return true;\n  try {\n    const granted = await PermissionsAndroid.request(\n      PermissionsAndroid.PERMISSIONS.WRITE_EXTERNAL_STORAGE,\n      { title: "Storage Permission", message: "App needs storage access to save files.", buttonPositive: "OK" }\n    );\n    return granted === PermissionsAndroid.RESULTS.GRANTED;\n  } catch { return false; }\n};\n\nexport const buildReportHTML = (p: {\n  title: string; accentColor: string; category: string;\n  totalRecords: number; headerCells: string; bodyRows: string;\n}): string => {\n  const d = new Date();\n  return ` + q + `<!DOCTYPE html><html><head><meta charset="utf-8"/><style>\n    *{margin:0;padding:0;box-sizing:border-box}\n    body{font-family:"Helvetica Neue",Arial,sans-serif;background:#f8fafc;padding:24px}\n    .report-header{background:linear-gradient(135deg,${p.accentColor},${p.accentColor}cc);border-radius:12px;padding:20px 24px;margin-bottom:20px;color:#fff}\n    .report-header h1{font-size:20px;font-weight:700;margin-bottom:4px}\n    .report-header p{font-size:11px;opacity:.85}\n    .stats-row{display:flex;gap:12px;margin-bottom:20px}\n    .stat-card{flex:1;background:#fff;border-radius:10px;padding:12px 16px;border:1px solid #e2e8f0}\n    .stat-label{font-size:10px;color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:4px}\n    .stat-value{font-size:22px;font-weight:700;color:${p.accentColor}}\n    .table-container{background:#fff;border-radius:12px;overflow:hidden;border:1px solid #e2e8f0}\n    table{width:100%;border-collapse:collapse}\n    thead tr{background:${p.accentColor}}\n    th{color:#fff;padding:10px 8px;text-align:left;font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.6px}\n    td{padding:9px 8px;font-size:10px;color:#0f172a;vertical-align:top;word-break:break-word;border-bottom:1px solid #f1f5f9}\n    .row-even td{background:#f8fafc}.row-odd td{background:#fff}\n    .footer{margin-top:16px;text-align:center;font-size:9px;color:#94a3b8}\n  </style></head><body>\n  <div class="report-header"><h1>${p.title} Report</h1>\n    <p>Generated on ${d.toLocaleString()} &nbsp;&bull;&nbsp; Total: ${p.totalRecords} records</p></div>\n  <div class="stats-row">\n    <div class="stat-card"><div class="stat-label">Total Records</div><div class="stat-value">${p.totalRecords}</div></div>\n    <div class="stat-card"><div class="stat-label">Export Date</div><div class="stat-value" style="font-size:13px;margin-top:4px">${d.toLocaleDateString()}</div></div>\n    <div class="stat-card"><div class="stat-label">Category</div><div class="stat-value" style="font-size:13px;margin-top:4px">${p.category.toUpperCase()}</div></div>\n  </div>\n  <div class="table-container"><table><thead><tr>${p.headerCells}</tr></thead><tbody>${p.bodyRows}</tbody></table></div>\n  <div class="footer">Generated by WebbiTech Lead Tracker &nbsp;&bull;&nbsp; Confidential</div>\n</body></html>` + q + `;\n};\n\nexport const saveBase64AndShare = async (base64Data: string, fileName: string, mimeType: string): Promise<void> => {\n  if (Platform.OS === "android") {\n    const cachePath = ` + q + `${RNBlobUtil.fs.dirs.CacheDir}/${fileName}` + q + `;\n    await RNBlobUtil.fs.writeFile(cachePath, base64Data, "base64");\n    if (Platform.Version >= 29) {\n      await RNBlobUtil.MediaCollection.copyToMediaStore({ name: fileName, parentFolder: "", mimeType }, "Download", cachePath);\n    } else {\n      const ok = await requestStoragePermission();\n      if (!ok) { ToastAndroid.show("Storage permission denied", ToastAndroid.SHORT); return; }\n      await RNFS.copyFile(cachePath, ` + q + `${RNFS.DownloadDirectoryPath}/${fileName}` + q + `);\n    }\n    ToastAndroid.show(` + q + `${fileName} Downloaded` + q + `, ToastAndroid.SHORT);\n    await Share.open({ url: ` + q + `file://${cachePath}` + q + `, type: mimeType, filename: fileName, failOnCancel: false });\n  } else {\n    const docPath = ` + q + `${RNFS.DocumentDirectoryPath}/${fileName}` + q + `;\n    await RNFS.writeFile(docPath, base64Data, "base64");\n    await Share.open({ url: ` + q + `file://${docPath}` + q + `, type: mimeType, filename: fileName, failOnCancel: false });\n  }\n};\n\nexport const saveTextAndShare = async (content: string, fileName: string, mimeType: string): Promise<void> => {\n  if (Platform.OS === "android") {\n    const cachePath = ` + q + `${RNBlobUtil.fs.dirs.CacheDir}/${fileName}` + q + `;\n    await RNBlobUtil.fs.writeFile(cachePath, content, "utf8");\n    if (Platform.Version >= 29) {\n      await RNBlobUtil.MediaCollection.copyToMediaStore({ name: fileName, parentFolder: "", mimeType }, "Download", cachePath);\n    } else {\n      const ok = await requestStoragePermission();\n      if (!ok) { ToastAndroid.show("Storage permission denied", ToastAndroid.SHORT); return; }\n      await RNFS.copyFile(cachePath, ` + q + `${RNFS.DownloadDirectoryPath}/${fileName}` + q + `);\n    }\n    ToastAndroid.show(` + q + `${fileName} Downloaded` + q + `, ToastAndroid.SHORT);\n    await Share.open({ url: ` + q + `file://${cachePath}` + q + `, type: mimeType, filename: fileName, failOnCancel: false });\n  } else {\n    const docPath = ` + q + `${RNFS.DocumentDirectoryPath}/${fileName}` + q + `;\n    await RNFS.writeFile(docPath, content, "utf8");\n    await Share.open({ url: ` + q + `file://${docPath}` + q + `, type: mimeType, filename: fileName, failOnCancel: false });\n  }\n};\n\nexport const savePDFAndShare = async (sourcePath: string, fileName: string): Promise<void> => {\n  if (Platform.OS === "android") {\n    const cachePath = ` + q + `${RNBlobUtil.fs.dirs.CacheDir}/${fileName}` + q + `;\n    await RNFS.copyFile(sourcePath, cachePath);\n    if (Platform.Version >= 29) {\n      await RNBlobUtil.MediaCollection.copyToMediaStore({ name: fileName, parentFolder: "", mimeType: "application/pdf" }, "Download", cachePath);\n    } else {\n      const ok = await requestStoragePermission();\n      if (!ok) { ToastAndroid.show("Storage permission denied", ToastAndroid.SHORT); return; }\n      await RNFS.copyFile(cachePath, ` + q + `${RNFS.DownloadDirectoryPath}/${fileName}` + q + `);\n    }\n    ToastAndroid.show("PDF Downloaded to Downloads folder", ToastAndroid.SHORT);\n    await Share.open({ url: ` + q + `file://${cachePath}` + q + `, type: "application/pdf", filename: fileName, failOnCancel: false });\n  } else {\n    const docPath = ` + q + `${RNFS.DocumentDirectoryPath}/${fileName}` + q + `;\n    await RNFS.copyFile(sourcePath, docPath);\n    await Share.open({ url: ` + q + `file://${docPath}` + q + `, type: "application/pdf", filename: fileName, failOnCancel: false });\n  }\n};\n\nexport type ExportConfig = {\n  data: any[]; title: string; accent: string; headers: string[];\n  toExcelRow: (item: any) => Record<string, any>;\n  toCsvCells: (item: any) => any[];\n  toCopyLine: (item: any, index: number) => string;\n  toPrintCells: (item: any, index: number) => string;\n};\n\nexport const exportCopy = (cfg: ExportConfig): void => {\n  if (!cfg || cfg.data.length === 0) { ToastAndroid.show("No records to copy", ToastAndroid.SHORT); return; }\n  const text = cfg.data.map((item, i) => cfg.toCopyLine(item, i)).join("\\n\\n---\\n\\n");\n  Clipboard.setString(text);\n  ToastAndroid.show(` + q + `Copied ${cfg.data.length} record(s) to clipboard` + q + `, ToastAndroid.SHORT);\n};\n\nexport const exportExcel = async (cfg: ExportConfig): Promise<void> => {\n  const exportData = cfg.data.map((item) => cfg.toExcelRow(item));\n  const worksheet = XLSX.utils.json_to_sheet(exportData);\n  const workbook = XLSX.utils.book_new();\n  XLSX.utils.book_append_sheet(workbook, worksheet, cfg.title);\n  const wbout = XLSX.write(workbook, { type: "base64", bookType: "xlsx" });\n  const fileName = ` + q + `${cfg.title.replace(/ /g, "_")}_${Date.now()}.xlsx` + q + `;\n  await saveBase64AndShare(wbout, fileName, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");\n};\n\nexport const exportCSV = async (cfg: ExportConfig): Promise<void> => {\n  const escapeCSV = (v: any) => ` + q + `"${String(v ?? "").replace(/"/g, "\\\"\\\"")}"` + q + `;\n  const headerRow = cfg.headers.slice(1).join(",");\n  const dataRows = cfg.data.map((item) => cfg.toCsvCells(item).map(escapeCSV).join(","));\n  const csvContent = [headerRow, ...dataRows].join("\\n");\n  const fileName = ` + q + `${cfg.title.replace(/ /g, "_")}_${Date.now()}.csv` + q + `;\n  await saveTextAndShare(csvContent, fileName, "text/csv");\n};\n\nconst buildAndGeneratePDF = async (cfg: ExportConfig, category: string): Promise<{filePath: string; fileName: string} | null> => {\n  const headerCells = cfg.headers.map(h => ` + q + `<th>${h}</th>` + q + `).join("");\n  const bodyRows = cfg.data.map((item, i) => ` + q + `<tr class="${i % 2 === 0 ? "row-even" : "row-odd"}">${cfg.toPrintCells(item, i)}</tr>` + q + `).join("");\n  const html = buildReportHTML({ title: cfg.title, accentColor: PDF_ACCENT, category, totalRecords: cfg.data.length, headerCells, bodyRows });\n  const rawFileName = ` + q + `${cfg.title.replace(/ /g, "_")}_${Date.now()}` + q + `;\n  const fileName = ` + q + `${rawFileName}.pdf` + q + `;\n  const pdf = await generatePDF({ html, fileName: rawFileName, directory: "Documents" });\n  if (pdf.filePath) return { filePath: pdf.filePath, fileName };\n  return null;\n};\n\nexport const exportPrint = async (cfg: ExportConfig, category: string): Promise<void> => {\n  const result = await buildAndGeneratePDF(cfg, category);\n  if (result) { await savePDFAndShare(result.filePath, result.fileName); }\n  else { ToastAndroid.show("Failed to generate PDF", ToastAndroid.SHORT); }\n};\n\nexport const exportPDF = async (cfg: ExportConfig, category: string): Promise<void> => {\n  const result = await buildAndGeneratePDF(cfg, category);\n  if (result) { await savePDFAndShare(result.filePath, result.fileName); }\n  else { ToastAndroid.show("Failed to export PDF", ToastAndroid.SHORT); }\n};\n`; fs.writeFileSync("src/utils/exportUtils.ts", content, "utf8"); console.log("Done");
